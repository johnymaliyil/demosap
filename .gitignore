node_modules/
dist/
archive.zip

# Fiori tools deploy credentials / local overrides — never commit these
.env
.fioritoolsrc

# Auto-fetched real service metadata (may contain system-specific details)
webapp/localService/metadata.xml

# Other
.DS_Store
